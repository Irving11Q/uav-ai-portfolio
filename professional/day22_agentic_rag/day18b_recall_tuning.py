#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""
═══════════════════════════════════════════════════════════════
  Day18-b：召回调优 —— 该召回的没召回、不该答的乱答，怎么救
═══════════════════════════════════════════════════════════════

【这个文件回答什么问题】
  18a 量出基线后，会看见两个具体的病：
    病①  用户说"能飞多久"，手册写"续航" —— 措辞对不上，查不回来
    病②  手册里根本没有的问题，系统也硬答 —— 满嘴跑火车
  今天开三副药，逐副验证。

  技巧①  查询改写（Query Rewriting）：口语 → 手册术语
  技巧②  混合检索（Hybrid Search）+ RRF 融合：字频 + 语义
  技巧③  分数阈值（Score Threshold）：低于阈值就拒答，敢说"我不知道"

【怎么读这个文件】按顺序读这 3 部分：

  第 4 部分：技巧① 查询改写（术语表 + 大模型改写两条路）
  第 5 部分：技巧② 混合检索 + RRF 融合
  第 6 部分：技巧③ 分数阈值

  （编号沿用原 Day18，方便你对照未拆分的版本）

【运行方式】
    cd w4_rag
    python day18b_recall_tuning.py

【依赖】day18a_baseline_eval.py（本文件从它 import 评测集和检索底座）
【下一步】day18c_rerank_mmr.py —— 重排序和去重值不值得上

【⚠️ 先剧透一个反直觉的结论】
  技巧② 单独用，成绩反而会**下降**（Top-1 从 4/6 掉到 3/6）。
  代码里会如实跑出来、如实解释，不会藏。
"""

# ════════════════════════════════════════════════════════════════
# 环境与复用 —— 从 Day18-a 借"尺子"和"检索底座"
# ════════════════════════════════════════════════════════════════

import os
# 【解释】os = 操作系统相关功能，拼路径要用。

import sys
# 【解释】sys = Python 解释器相关，改模块搜索路径要用。

import re
# 【解释】re = 正则表达式：解析大模型返回的数字编号时要用到。

import requests
# 【解释】requests = 发 HTTP 请求，调大模型 API 改写提问要用。

_HERE = os.path.dirname(os.path.abspath(__file__))
# 【解释】本文件所在目录（w4_rag）。
if _HERE not in sys.path:
    # 【解释】避免重复插入。
    sys.path.insert(0, _HERE)
    # 【解释】插到最前面，优先搜索。

# ── 从 18a 借来"尺子"和"底座" ──
# 【解释】这就是真实项目的写法：昨天写的 18a，今天是你的工具箱。
#         不在本文件里重复定义 EVAL_SET / build_index / evaluate，
#         而是 import 过来 —— 保证三份文件量的是同一把尺子。
try:
    from day18a_baseline_eval import (
        d17,
        # 【解释】Day17 模块（18a 已经处理过"整包版/拆分版"两种降级）。
        EVAL_SET, OFF_TOPIC_QUESTIONS,
        # 【解释】评测集：6 道在题 + 3 道离题（离题那 3 道是用来测拒答的）。
        CHUNKS, TEXTS, IDS, ID2IDX, SECTIONS,
        # 【解释】手册块数据（8 块）。
        build_index,
        # 【解释】建 Chroma 库，返回 (collection, embedding 模式名)。
        semantic_scores, keyword_scores,
        # 【解释】两路召回：语义路、字频路。
        topk, evaluate,
        # 【解释】取前 k 名 / 在评测集上打分。
    )
except Exception as e:
    # 【解释】18a 不在同目录。
    print(f"⚠️  没能导入 day18a_baseline_eval（{type(e).__name__}），程序无法继续")
    # 【解释】今天强依赖 18a。
    print("    请把 day18a_baseline_eval.py 放在同目录")
    # 【解释】给出修复办法。
    sys.exit(1)
    # 【解释】退出程序。

# ── 让 Python 找得到 w3_ai 的 api_config.py（跨周复用，和 Day15/17/18a 一致）──
sys.path.insert(0, os.path.join(os.path.dirname(_HERE), "w3_ai"))
# 【解释】w4_rag 的上级目录是 src，再拼 w3_ai 就是配置所在处。
try:
    import api_config
    # 【解释】读 API 配置（BASE_URL / MODEL_NAME / API_KEY）。
    HAS_KEY = bool(api_config.API_KEY)
    # 【解释】有 key 才能调大模型做改写。
except Exception:
    # 【解释】没找到配置或配置报错（比如没设环境变量）。
    HAS_KEY = False
    # 【解释】降级：只跑术语表改写，不跑大模型改写。


# ════════════════════════════════════════════════════════════════
# 第 4 部分：技巧① 查询改写 —— 把用户的口语翻译成手册术语
# ════════════════════════════════════════════════════════════════
#
# 【问题】用户说"能飞多久"，手册写"续航"。两个词字面毫无重合，
#         语义模型能理解一部分，但不总是靠谱。
#
# 【思路】在检索之前，先把提问"翻译"成手册的措辞。
#         这一步叫 Query Rewriting / Query Expansion。
#
# 【两种做法】
#   做法 A：术语映射表（离线、零成本、可控）  ← 今天的主力
#   做法 B：让大模型改写（灵活、要花钱、有延迟）← 今天作为可选演示
#
# ⚠️ 工程经验：能靠规则解决的，就别上大模型。
#    术语表改一个词只要 1 毫秒、0 成本；大模型改写要 1 秒、要花钱、还可能改歪。
#    只有术语表覆盖不住（比如提问很长的多跳问题）才用大模型兜底。

# ── 术语映射表：用户口语 → 手册术语 ──
# 【解释】这张表怎么来的？做法是：把真实用户提问和手册术语放在一起比对，
#         挑出"说法不同但指同一件事"的词对。
#         真实项目里，这张表应该由业务专家维护，并且持续补充。
TERM_MAP = {
    "能飞多久": "续航",
    # 【解释】"能飞多久" = 手册里的"续航"。
    "飞多久": "续航",
    # 【解释】同上，更简短的说法。
    "多少分钟": "续航",
    # 【解释】问时间长度，同样指向续航。
    "掉电": "续航下降",
    # 【解释】用户说"掉电快"，手册说"续航下降"。
    "冬天": "低温",
    # 【解释】用户说季节，手册说温度条件。
    "胀起来": "鼓包",
    # 【解释】"胀起来"是口语，"鼓包"是手册原文。这是最关键的一条。
    "胀": "鼓包",
    # 【解释】单字也要覆盖，防止用户只说"电池胀了"。
    "多少伏": "电压",
    # 【解释】用户问"多少伏"，手册章节叫"电压管理"。
    "保存": "存放 存储电压",
    # 【解释】"保存"对应手册的"存放"和"存储电压"两个概念。
}


def expand_query(question):
    """
    术语扩展：把提问里的口语词，替换/补充成手册术语。

    做法：原提问 + 追加术语（而不是替换）。
    为什么是追加而不是替换？因为原提问本身也有信息量，
    丢掉可能把模型搞糊涂 —— "追加"是更安全的选择。
    """
    # 【解释】这是今天投入产出比最高的一招：一张 9 行的表，Top-1 直接 +1。

    extra = []
    # 【解释】准备追加的术语列表。
    for spoken, formal in TERM_MAP.items():
        # 【解释】遍历术语表的每一项。
        if spoken in question:
            # 【解释】提问里出现了这个口语词。
            extra.append(formal)
            # 【解释】把对应术语记下来。
    if not extra:
        # 【解释】一个都没命中，说明提问已经是手册措辞了。
        return question
        # 【解释】原样返回。
    return question + " " + " ".join(extra)
    # 【解释】原提问后面追加术语，用空格隔开。
    #         例："电池能飞多久" → "电池能飞多久 续航"


def rewrite_with_llm(question):
    """
    用大模型改写提问（可选方案，需要 API Key）。

    什么时候才需要它？
    提问很长、很绕、或者包含多个意图时，术语表覆盖不住，
    这时候让大模型把它"翻译"成检索友好的短查询。
    """
    # 【解释】这是做法 B，作为扩展视野用；默认流程里不依赖它。

    if not HAS_KEY:
        # 【解释】没配 key。
        return question
        # 【解释】原样返回，不改写。

    prompt = (
        "你是无人机技术文档的检索助手。\n"
        "请把用户的口语化提问，改写成适合在手册里检索的短语。\n"
        "要求：只输出改写结果，不要解释；保留关键技术词；不超过 20 字。\n\n"
        f"用户提问：{question}"
    )
    # 【解释】提示词三要素：角色、任务、输出格式约束。
    #         "只输出改写结果" 这句很关键 —— 不约束的话模型会啰嗦一堆，解析就麻烦了。

    try:
        r = requests.post(
            api_config.BASE_URL + "/chat/completions",
            # 【解释】接口地址（api_config 里按 PROVIDER 配好的）。
            headers={"Authorization": "Bearer " + api_config.API_KEY,
                     "Content-Type": "application/json"},
            # 【解释】鉴权头 + JSON 格式声明。
            json={"model": api_config.MODEL_NAME,
                  "messages": [{"role": "user", "content": prompt}],
                  "temperature": 0.0},
            # 【解释】temperature=0：改写任务要稳定，不要创造性。
            timeout=30,
            # 【解释】超时保护，网络卡住不能一直等。
        )
        d = r.json()
        # 【解释】解析返回的 JSON。
        if "choices" not in d:
            # 【解释】Day11 加过的防御：接口报错时不会有 choices 字段。
            return question
            # 【解释】失败就退回原提问，绝不因为改写失败而中断检索。
        out = d["choices"][0]["message"]["content"].strip()
        # 【解释】取出模型回复的文本并去掉首尾空白。
        return out if out else question
        # 【解释】空结果也退回原提问。
    except Exception:
        # 【解释】网络异常、超时、解析失败，都走这里。
        return question
        # 【解释】兜底：原样返回。⚠️ 改写是"锦上添花"，绝不能成为故障点。



# ════════════════════════════════════════════════════════════════
# 第 5 部分：技巧② 混合检索 + RRF 融合
# ════════════════════════════════════════════════════════════════
#
# 【问题】语义检索擅长"意思"，字频检索擅长"字面"。
#         单独用哪个都有盲区：
#           · 语义检索对专有名词、型号、数字不敏感
#           · 字频检索对用户换个说法就抓瞎
#
# 【思路】两路都召回，再把结果融合起来。
#
# 【怎么融合？为什么不能用分数直接加权？】
#   ⚠️ 这是新手最容易踩的坑：
#   语义相似度是 0~1 的余弦值，字频相似度是 0~1 的词频余弦值，
#   两者的数值分布完全不同（实测：语义 0.55~0.74，字频 0.14~0.43）。
#   直接加权平均 = 让字频那一票被数值差异淹没，等于没融合。
#
#   正确做法是 **RRF（Reciprocal Rank Fusion，倒数排名融合）**：
#   不看分数，只看名次。某个块在第 r 名，就得分 1/(k + r)。
#   两路的名次分相加，总分高的排前面。
#   这样完全绕开了"两路分数不可比"的问题。

RRF_K = 60
# 【解释】RRF 的平滑常数。k 越大，名次靠前的优势越不明显（结果越平）。
#         60 是原始论文推荐值，也是工业界最常用的默认参数。


def rrf_fuse(list_of_score_dicts, k=RRF_K):
    """
    RRF 融合：把多路检索结果合成一路。

    参数 list_of_score_dicts 是 [{下标: 分数}, {下标: 分数}, ...]，
    函数内部只看每路内部的排名，不看分数本身。
    """
    # 【解释】这是今天第二个关键技巧，也是真实 RAG 系统的标配。

    fused = {}
    # 【解释】累加后的得分表：{下标: RRF得分}。
    for scores in list_of_score_dicts:
        # 【解释】逐路处理。
        ranked = sorted(scores.items(), key=lambda kv: -kv[1])
        # 【解释】把这一路的结果按分数从高到低排序。
        for rank, (idx, _score) in enumerate(ranked):
            # 【解释】rank 从 0 开始，是该块在这一路的名次。
            #         _score 用下划线开头，表示"故意不使用这个变量"。
            fused[idx] = fused.get(idx, 0.0) + 1.0 / (k + rank + 1)
            # 【解释】核心公式：名次越靠前（rank 越小），加的分越多。
            #         第 1 名加 1/61，第 2 名加 1/62 …… 衰减很平缓，
            #         这正是 RRF 的特点：不迷信第 1 名，但也不无视它。
    return fused
    # 【解释】返回融合后的分数表。


def hybrid_search(coll, question, use_expansion=True, n_results=8):
    """
    混合检索：字频 + 语义，RRF 融合。
    """
    # 【解释】把前面几块拼起来：先改写提问，再两路召回，最后融合。

    q = expand_query(question) if use_expansion else question
    # 【解释】是否启用术语扩展（第 9 部分要靠这个开关做对比实验）。

    s_sem = semantic_scores(coll, q, n_results)
    # 【解释】第一路：语义检索。
    s_kw = keyword_scores(q, n_results)
    # 【解释】第二路：字频检索。

    return rrf_fuse([s_sem, s_kw])
    # 【解释】融合后返回 {下标: RRF得分}。



# ════════════════════════════════════════════════════════════════
# 第 6 部分：技巧③ 分数阈值 —— 敢说"我不知道"
# ════════════════════════════════════════════════════════════════
#
# 【问题】RAG 最招人烦的失败不是答错，而是**胡编**：
#         手册里明明没有，它照样一本正经编一段。
#
# 【思路】检索阶段就设一道关：最高分都低于阈值 → 判定"手册里没有" → 直接拒答。
#
# 【阈值怎么定？不能拍脑袋】
#   我在本手册上实测了两批提问的"最高相似度"：
#
#       手册里有的提问（6 条）     最低 0.574
#       手册里没有的提问（3 条）   最高 0.466
#
#   中间那段空白（0.466 ~ 0.574）就是安全区，取 0.50 一刀切开。
#   ⚠️ 这个 0.50 只对"这份手册 + 这个 embedding 模型"成立，
#      换数据或换模型必须重新测 —— 阈值是量出来的，不是抄来的。

SCORE_THRESHOLD = 0.50
# 【解释】判定阈值：最高【语义相似度】< 0.50 就认为"手册里没有相关内容"。
#         ⚠️ 注意这个阈值是跟"语义余弦相似度"配套的（0~1 之间），
#            不是跟 RRF 融合分配套的 —— 混用会失效，原因见下面函数里的说明。


def search_with_threshold(coll, question, threshold=SCORE_THRESHOLD, top_k=3):
    """
    带阈值的检索：最高相似度低于阈值就返回空（表示"查不到"）。

    返回 (命中列表, 是否触发拒答)。命中列表里带的是【语义相似度】，
    不是 RRF 融合分 —— 原因见下面第一条解释。
    """
    # 【解释】这个函数体现了 RAG 工程里非常重要的一条原则：
    #         **宁可说"不知道"，也不要编。**
    #         用户对"我不知道"的容忍度，远高于对"编得挺像但错了"的容忍度。

    # ⚠️⚠️ 这里有个非常隐蔽、我实测才发现的坑，务必看懂：
    #   RRF 融合分**不能**拿来当阈值！
    #   原因是 RRF 分数只取决于名次（第 1 名固定拿 1/61 ≈ 0.016），
    #   跟"这条到底有多相关"无关。8 块语料时满分也就 0.033 左右，
    #   拿它跟 0.50 比，永远不可能触发拒答 —— 阈值就成了摆设。
    #
    #   正确做法是**分工**：
    #     · 排序  → 用 RRF 融合分（哪家排得准听谁的）
    #     · 判定  → 用原始语义相似度（分数可比、有绝对含义）
    #   这也是工业界的通行做法：融合排序 + 独立的相关性打分。

    fused = hybrid_search(coll, question)
    # 【解释】第一步：混合检索给出排序（用 RRF 分）。

    sem = semantic_scores(coll, expand_query(question), len(CHUNKS))
    # 【解释】第二步：单独算一遍语义相似度，用作"相关性判定"的依据。
    #         注意用的是改写后的提问，和排序时保持一致。

    ordered_idx = [i for i, _ in topk(fused, len(CHUNKS))]
    # 【解释】按融合分从高到低取出所有块的下标。

    picked = [(i, sem.get(i, 0.0)) for i in ordered_idx
              if sem.get(i, 0.0) >= threshold]
    # 【解释】第三步：按排序顺序走，只保留相似度达标的块。
    #         sem.get(i, 0.0)：取不到就当 0 分（不达标）。

    if not picked:
        # 【解释】一个达标的都没有 → 判定"手册里没有相关内容"。
        return [], True
        # 【解释】空列表 + 拒答标记。

    return picked[:top_k], False
    # 【解释】返回达标的前 k 个（带着可解释的语义相似度分数）。


def top_semantic_score(coll, question):
    """
    取混合检索排第 1 的块，返回 (下标, 语义相似度)。

    专门给"量分数分布"这一步用：排序按 RRF，报出来的分数用语义相似度。
    """
    # 【解释】和 search_with_threshold 保持同一套口径，
    #         否则"量的分数"和"实际判定的分数"对不上，阈值就白定了。

    fused = hybrid_search(coll, question)
    # 【解释】混合检索给出排序。
    sem = semantic_scores(coll, expand_query(question), len(CHUNKS))
    # 【解释】算语义相似度。
    ordered = topk(fused, 1)
    # 【解释】取第 1 名。
    if not ordered:
        # 【解释】没查到。
        return None, 0.0
        # 【解释】返回空。
    i = ordered[0][0]
    # 【解释】第 1 名的下标。
    return i, sem.get(i, 0.0)
    # 【解释】返回 (下标, 语义相似度)。



# ════════════════════════════════════════════════════════════════
# 主流程
# ════════════════════════════════════════════════════════════════

def main():
    """按 步骤 1→5 跑完三个召回调优技巧。"""

    print("=" * 66)
    print("  Day18-b：召回调优 —— 该召回的没召回、不该答的乱答")
    print("=" * 66)
    print(f"  大模型：{'已配置 ' + api_config.MODEL_NAME if HAS_KEY else '未配置（只跑术语表改写）'}")
    # 【解释】说明当前能力边界。

    # ── 步骤 1：建索引 ──
    print("\n" + "═" * 66)
    print("  步骤 1 / 5：建索引（复用 18a 的 build_index）")
    print("═" * 66)
    coll, mode = build_index()
    # 【解释】和 18a 用的是同一个库目录 chroma_db_day18，第二次跑会直接复用。

    # ── 步骤 2：看查询改写做了什么 ──
    print("\n" + "═" * 66)
    print("  步骤 2 / 5：技巧① 查询改写 —— 口语怎么变成手册术语")
    print("═" * 66)
    print("  用户说的话，和手册写的话，往往不是一个词：")
    print("\n" + f"  {'用户提问':<24}→  改写后")
    # 【解释】表头。
    print("  " + "─" * 54)
    # 【解释】分隔线。
    for q, _ in EVAL_SET:
        # 【解释】逐题展示。
        print(f"  {q:<24}→  {expand_query(q)}")
        # 【解释】打印改写前 → 改写后。
    print("  " + "─" * 54)
    # 【解释】分隔线。
    print("\n  💡 这张术语表只有 %d 行，成本是 1 毫秒、0 元、可控可维护。"
          % len(TERM_MAP))
    # 【解释】强调投入产出比 —— 这是今天性价比最高的一招。

    if HAS_KEY:
        # 【解释】有 key 就顺带演示大模型改写（术语表覆盖不到的说法靠它）。
        print("\n  【大模型改写】术语表漏掉的说法，交给大模型：")
        # 【解释】说明用途。
        for q in ["飞机充满电可以飞几分钟", "遥控器怎么对频"]:
            # 【解释】挑两个术语表不一定能完全覆盖的问法。
            print(f"     {q}")
            # 【解释】打印原提问。
            print(f"       → {rewrite_with_llm(q)}")
            # 【解释】打印大模型改写结果。
    else:
        # 【解释】没 key。
        print("\n  ⏭️  未配置 API Key，跳过大模型改写演示（术语表改写照常可用）")
        # 【解释】如实说明跳过原因。

    # ── 步骤 3：改写之后，提升多少 ──
    print("\n" + "═" * 66)
    print("  步骤 3 / 5：改写前后对比 —— 提升必须拿数据说话")
    print("═" * 66)
    b1, b3, total = evaluate("基线", lambda q: topk(semantic_scores(coll, q)))
    # 【解释】不改写。
    print()
    # 【解释】空一行，两张表分开。
    r1, r3, _ = evaluate("+改写", lambda q: topk(semantic_scores(coll, expand_query(q))))
    # 【解释】改写了再查。
    print(f"\n  → Top-1：{b1}/{total} 提升到 {r1}/{total}；"
          f"Top-3：{b3}/{total} 提升到 {r3}/{total}")
    # 【解释】把提升量摆出来。

    # ── 步骤 4：混合检索（含反面教材）──
    print("\n" + "═" * 66)
    print("  步骤 4 / 5：技巧② 混合检索 + RRF（⚠️ 单独用反而变差）")
    print("═" * 66)
    print("  RRF 融合公式：score = Σ 1/(k + rank)，k=%d" % RRF_K)
    # 【解释】先把公式摆出来。
    print("  为什么不用分数加权？因为 18a 已经量出两路分数不在同一量级。")
    # 【解释】回扣 18a 的结论。

    print()
    # 【解释】空行。
    h1, h3, _ = evaluate("混合(不改写)",
                         lambda q: topk(hybrid_search(coll, q, use_expansion=False)))
    # 【解释】⚠️ 关掉改写，单独看融合的效果 —— 这是今天的反面教材。

    print()
    # 【解释】空行。
    c1, c3, _ = evaluate("改写+混合",
                         lambda q: topk(hybrid_search(coll, q, use_expansion=True)))
    # 【解释】两个技巧组合起来。

    print("\n  📌 这一段最值得记住：")
    # 【解释】承上启下。
    print(f"     只用混合检索：Top-1 {h1}/{total}  ← 比基线 {b1}/{total} 还差")
    # 【解释】摆出反直觉的数据。
    print(f"     改写+混合：    Top-1 {c1}/{total}  ← 组合起来才对")
    # 【解释】对比。
    print("\n     原因：提问没被改写成手册措辞时，字频那一路基本命中不了，")
    # 【解释】解释原因。
    print("           融进去的就不是互补信息，而是噪声 —— 把好结果的名次搅乱了。")
    # 【解释】点明机理。
    print("     → 结论：技巧之间有依赖顺序，单独看没用的，组合起来可能是关键。")
    # 【解释】给出可迁移的结论。

    # ── 步骤 5：阈值定在哪 ──
    print("\n" + "═" * 66)
    print("  步骤 5 / 5：技巧③ 分数阈值 —— 先量区间，再定阈值")
    print("═" * 66)
    print("  【手册里有答案的】最高相似度：")
    # 【解释】先测"应该能查到"的。
    in_scores = []
    # 【解释】收集分数。
    for q, _ in EVAL_SET:
        # 【解释】逐题。
        i, score = top_semantic_score(coll, q)
        # 【解释】取第 1 名的【语义相似度】。
        if i is not None:
            # 【解释】查到了。
            in_scores.append(score)
            # 【解释】记录。
            print(f"     {q:<24}{score:.3f}")
            # 【解释】打印。

    print("  【手册里没有的】最高相似度：")
    # 【解释】再测"应该查不到"的。
    out_scores = []
    # 【解释】收集分数。
    for q in OFF_TOPIC_QUESTIONS:
        # 【解释】逐题。
        i, score = top_semantic_score(coll, q)
        # 【解释】同样取第 1 名。
        if i is not None:
            # 【解释】当然会"查到"，只是不该信。
            out_scores.append(score)
            # 【解释】记录。
            print(f"     {q:<24}{score:.3f}  （最接近的是「{SECTIONS[i]}」）")
            # 【解释】打印错配到哪去了 —— 这就是"没阈值就会胡编"的现场证据。

    if in_scores and out_scores:
        # 【解释】两边都有数据才谈得上定阈值。
        gap_lo = max(out_scores)
        # 【解释】离题组最高分。
        gap_hi = min(in_scores)
        # 【解释】在题组最低分。
        print(f"\n  📏 离题最高 {gap_lo:.3f}  <  在题最低 {gap_hi:.3f}")
        # 【解释】把这段"安全区"摆出来。
        print(f"     当前阈值取 {SCORE_THRESHOLD}，落在两者之间")
        # 【解释】说明阈值依据。
        if gap_lo >= SCORE_THRESHOLD or gap_hi <= SCORE_THRESHOLD:
            # 【解释】阈值没落在空隙里。
            print("     ⚠️ 当前阈值与实测数据不匹配，请按上面的区间重新调整")
            # 【解释】提醒校准，而不是假装没问题。

        print("\n  【拒答实测】")
        # 【解释】用离题提问试拒答。
        for q in OFF_TOPIC_QUESTIONS:
            # 【解释】逐题。
            hits, refused = search_with_threshold(coll, q)
            # 【解释】带阈值的检索。
            mark = "🚫 已拒答" if refused else "⚠️ 未拒答"
            # 【解释】判定标记。
            print(f"     {q:<24}{mark}")
            # 【解释】打印。

    # ── 收尾 ──
    print("\n" + "═" * 66)
    print("  【读完之后】Day18-b 你学到了什么")
    print("═" * 66)
    print("""
  ✅ 查询改写是投入产出比最高的一招（一张术语表，先做它）
  ✅ 理解 RRF 为什么比"分数加权"靠谱（两路分数不可比）
  ✅ 亲眼看到"多路召回不是越多越好"—— 弱路会稀释强路
  ✅ 阈值是量出来的，不是拍脑袋定的；换语料换模型必须重测
  ✅ 判定要用【原始相似度】，不能用 RRF 融合分（融合分只反映名次）

  ⚠️ 三个容易踩的坑
    · 查询改写失败要能兜底退回原提问，绝不能因此中断检索
    · 术语表要持续维护：新手册、新说法进来了就要补
    · 阈值只对"当前语料 + 当前 embedding"有效

  下一步 → day18c_rerank_mmr.py
           重排序和 MMR 去重，这两个"高级技巧"到底值不值得上。
""")
    # 【解释】学习路线图。


if __name__ == "__main__":
    # 【解释】直接运行本文件时才跑 main()，被 18c/18d import 时不跑。
    main()
    # 【解释】启动主流程。
