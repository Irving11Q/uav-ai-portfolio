# 面试讲解稿 · 无人机 AI 应用开发作品集

> 用途：面试前把每个项目「讲透」。每个项目按 **① 是什么 / ② 我做了什么 / ③ 踩的坑(带数字) / ④ 能抛的亮点数字 / ⑤ 面试官可能追问 + 怎么答** 组织。
> 重点放在 W3–W5（AI 应用/工程岗真正考的 RAG 全链路、Agent、评测）。W1–W2 是嵌入式背景，一句话带过。

---

## 0. 一句话总纲（开场先说这个）

「我用一个 8 周的学习线，从 PySide6 界面、串口上位机，一路做到大模型 API、RAG 知识库、数据分析 Agent、多 Agent 协作。
核心想证明一件事：**LLM 不是拿来直接用的，而是包一层工程——检索、工具、调度——让它『不瞎编、算得准、会分工』。**
这条线里同一个飞行数据集被反复复用，从『画出来』到『喂给模型回答』，正好是一个 AI 应用落地的全过程。」

---

## 1. W1–W2 嵌入式背景（一句话带过）

- **W1 PySide6**：控件/布局/信号槽、QTimer 周期刷新、QThread 后台任务不卡 UI、实时曲线（pyqtgraph）。
- **W2 串口上位机**：从串口读无人机飞行参数（电压/电流/温度/高度），解析逗号分隔行，实时绘图。
- **踩坑**：后台线程直接刷 UI 会崩 → 用 `Signal` 把进度抛回主线程。
- **亮点数字**：W2 那 23 行真实串口日志，后面 W5 数据分析 Agent 直接拿来用（真实数据，不是造的）。
- **追问「你嵌入式到什么程度？」** → 答：「STM32（洋桃1号）+ PySide6 上位机 + 串口全链路自己打通过；但求职方向是 AI 应用/工程，不是嵌入式算法，这部分是背景加分。」

---

## 2. W3 大模型 API + Function Calling（Agent 的基石）

### day10–day12：首调 DeepSeek → 自封装 Client → 多轮上下文
- **做了什么**：用 `requests` 直连 `/chat/completions`，维护对话历史让模型「记得上一轮」；把 Key 放环境变量不硬编码。
- **亮点**：OpenAI 兼容协议 → 换模型只改 base_url/key/model 三个量（day11 抽成 `api_config.py` 的 PROVIDER 开关）。
- **追问「为什么不直接用官方 SDK？」** → 答：「早期手写 requests 是为了搞懂协议本身；后来知道 LangChain 适配器也是包这层，原理一样。」

### day13：Function Calling（**重点，Agent 起点**）
- **做了什么**：给模型两个真工具 `get_weather` / `estimate_flight_time`，把 `tools` spec 发给模型，解析它的 `tool_calls` → 本地执行 → 用 `role="tool"`+`tool_call_id` 把结果喂回 → 2 轮循环出最终答案。
- **踩的坑（讲透关键）**：手写循环只能跑一轮就 `return`，导致多步任务断在第一步 → 改成模型返回的工具结果再问一轮，循环 2 圈才收敛。
- **能抛的数字**：模型用工具返回的容量（3.5Ah）而非标称 5Ah 算续航 → 得 8.4 分钟；若用标称 5Ah 会得 12 分钟，**高估 43%**。→ 证明「让模型用工具的真实返回值，而不是凭记忆/标称值」。
- **追问「Function Calling 和 Agent 什么关系？」** → 答：「Function Calling 是『模型能调你的函数』，Agent 是在此之上加『决定调不调、调完再想』的循环和状态。day13 是地基，day21 才完整成 Agent。」

### day14：多模型对比
- **做了什么**：同问题跑 DeepSeek / 智谱 GLM，对比中文、速度、价格。结论：glm-4-flash 免费额度适合学习，deepseek-chat 中文好又便宜。

---

## 3. W4 RAG 全链路（**重点，AI 应用岗最高频**）

### day15：朴素 RAG 从零写（不依赖库）
- **做了什么**：手写切块→向量化（字符频率词表）→余弦相似度 Top-3→拼进 Prompt。对比「有检索」vs「直接问」，**检索让幻觉消失**。
- **亮点**：向量化/检索全离线，只有生成那步调 API；没 Key 也能跑前半段。

### day16：切块策略决定检索上限（**讲透关键**）
- **做了什么**：同一本手册三种切法——按段落 / 固定窗口(60字+15重叠) / 按标题(`##`)。验证「电池温度太高会怎样」命中哪块。
- **踩的坑（核心 insight）**：基线 Top-1 只有 **2/4（50%）**；问「电池能飞多久」时 8 块全挤在 0.50–0.63、极差仅 0.12，正确答案排第 6 → **病因是块太大导致向量平均化**。→ 回 day16 重切比调任何参数都管用。

### day17–day18：Chroma 向量库 + 检索调优
- **做了什么**：day17 文字→数字（SentenceTransformer 接 bge-small-zh）→ Chroma 存与找→拼 Prompt 带【引用】。day18 在 bge 基线上做检索调优（query 指令前缀、阈值、chunk 大小）。
- **踩的坑**：bge 的 query 指令前缀「官方最佳实践也要自己验证」——实测**不加 2/4、加了 1/4（更差）**，所以本项目不用前缀。
- **环境坑（可讲）**：`HF_HUB_OFFLINE=1` 设了仍去连 huggingface.co 重试 5 次×20s；最稳是**把本地快照路径直接喂给 SentenceTransformer 零网络秒开**（day20 的 `find_local_model`）。

### day19：带引用的 QA 系统 + 检索评测（**重点，考评测集工程化**）
- **做了什么**：检索器守住统一接口 `retrieve(q,k)->[(标题,正文,分数)]`，所以**换真检索只改 main() 一行、350 行界面零改动**（这是工程解耦的范例）。
- **能抛的数字（评测集）**：Top-1 **5/6**、Top-3 **6/6**、离题拒答 **3/3**、误拒 **0**（阈值 0.50 / bge-small-zh）。→ 说明我会建小评测集、看命中率而不是只跑通。
- **追问「怎么衡量 RAG 好不好？」** → 答：「我建了 6 条带标准答案的题，分命中率(Top-1/3)、拒答率（该拒的拒没拒）、误拒率（不该拒的拒了没）。光『能回答』不够，要看这几项。」

### day20：用 LangChain 重写
- **做了什么**：把 day17 的 RAG 用 LangChain 重写（适配器继承 Embeddings 接中文 BGE + LCEL 管道 + RunnableLambda 接自研 LLM）。验证切块互验 8==8。
- **追问「LangChain 和手写怎么选？」** → 答：「学习期手写搞懂原理；生产用 LangChain 省事，但要知道它在包什么。两种都写过。」

---

## 4. W5 Agent（**重点，当前最强卖点**）

### day21：单 Agent（LangGraph ReAct）
- **做了什么**：用 `build_agent(model, tools)` 搭图，模型循环调工具。两个工具**故意串行依赖**（电池体检出可用容量→续航估算用它算）。
- **能抛的数字**：同 day13 的 3.5Ah vs 5Ah、8.4 vs 12 分钟（高估 43%）。
- **记忆**：day21c 加 checkpointer + thread_id——无记忆时模型编造容量（4.25/4.76 每次不同）也不承认忘了；同 thread_id 正确传 3.5。→ 证明「多轮 Agent 必须管状态」。

### day22：Agentic RAG + CRAG（**重点**）
- **做了什么**：把检索器包成工具，模型**自己决定**何时检索、检索什么；再加 guard 节点（领域关键词命中→强制检索）堵「模型跳过工具瞎编」的洞。
- **踩的坑（讲透关键）**：
  1. `bind_tools` 和 `ToolNode` 必须收同一份工具列表，否则模型调的工具执行节点不认识 → 静默 fallback 到瞎编。
  2. 小模型会跳过工具瞎编「通常 1 年保修」→ 关键词 guardrail 强制检索后答「手册没提」。
  3. `temperature=0` 才稳定（0.3 时同问题时灵时不灵）。
  4. 死变量 `q = question` 只在兜底分支触发 → 那条分支最不被测，必须主动逼它跑一次。
- **追问「RAG 和 Agentic RAG 区别？」** → 答：「朴素 RAG 检索写死、每次都查；Agentic RAG 让模型判断要不要查、查完不好还能改写 query 重查（CRAG 的 grade 节点）。但『模型没调工具』这种失败，grade 发现不了，得靠代码层 guard 兜底——这是两种失败要两种机制。」

### day23：数据分析 Agent（**重点**）
- **做了什么**：四个 pandas 工具（peek/describe/filter_rows/correlate）+ 画图工具；自然语言→模型决定调哪个→数字/图出来。
- **踩的坑（核心对照实验）**：对照组「把整张表塞进 Prompt 让模型数」→ 模型数「电压<23.5V 有几条」=**3（错）**；实验组给 pandas 工具算 =**5（对）**。→ **LLM 算数不可靠、但决定算什么很擅长**。
- **能抛的数字**：温度>55℃ 共 **4 次**；电流 vs 电压 相关系数 **0.161（几乎没有相关）**。
- **踩的坑（工程）**：`_resolve_col` 列名模糊匹配——模型说「电压」而非「电压V」，不匹配就 KeyError→模型以为没这列开始瞎答；`api_config` 变量叫 `MODEL_NAME` 不是 `MODEL`，写错 `from ... import MODEL` 整块静默降级（有 Key 当没 Key）；CSV 找不到必须 `sys.exit(1)` 而非 `return`（否则退出码 0 骗过检查）。
- **亮点**：同一张 LangGraph 图（day21 那张），Day22/23 换工具=换应用，**图一行没改**。

### day24：多 Agent 协作（**当前收官**）
- **做了什么**：supervisor（调度者）LLM 路由 data/manual/FINISH；两个 worker 子图——data_worker 复用 day23 工具，manual_worker 用 **bge 内存索引**检索《电池手册》（不落盘 Chroma，目录自包含）。子图协作 = `graph.invoke(worker)` 把消息合并回共享状态。
- **踩的坑**：① 子图协作本质就是 dispatcher `.invoke(worker)` + 合并消息；② 路由**必须有终止态**(`FINISH` + step 上限)，否则 worker 间无限互甩；③ 语料只有 7 段时用内存余弦即可，不必上 Chroma（检索后端匹配语料规模）。
- **能抛的数字**：Day21 那张图本周被复用 **4 次**（21/22/23/今天两个 worker）。调度者实测正确路由「锂电池温度…」→manual（调 search_manual）、「温度>55度几次」→data（filter_rows 得 4 次）。
- **追问「多 Agent 和单 Agent 怎么选？」** → 答：「任务能被一个 Agent + 一套工具干完，就单 Agent（day23）；任务跨多个专业领域、需要分工，才上 supervisor 把『分工』和『干活』拆开（day24）。不是越多 Agent 越好。」

---

## 5. 横切问题（几乎必问）

- **「你的项目都是学习 demo，生产怎么落地？」** → 答：「demo 抽象了 production 的三个真问题——① 检索质量靠切块+评测集（day16/19）；② 工具/调度解耦，换模型换检索只改一行（day19 接口、day21 图）；③ 小模型会跳过工具/瞎编，靠 guard+温度0+评测兜底（day22）。工程结构和 production 一致，只是规模小。」
- **「为什么不用现成框架一把梭？」** → 答：「RAG 全链路和 Agent 图我都手写过一遍（day15/21），再上 LangChain/LangGraph 就清楚它在包什么、出问题知道去哪改。框架是加速器不是黑盒。」
- **「评测怎么做？」** → 答：「每个阶段都有小评测集：RAG 看 Top-1/3+拒答/误拒（day19）；Agent 看『模型有没有跳过工具瞎编』（day22）；数据分析看『模型数数 vs 工具数数』（day23）。我相信可量化的指标，不是『跑通就行』。」
- **「接下来想补什么？」** → 答：「FastAPI 把 Agent 包成服务 + Docker 化部署，以及更成体系的效果评测（这块我自己也标了是短板，正在补）。」

---

## 6. 一句话收尾

「这条线我想展示的不是『我会调 API』，而是**把 LLM 包进工程的能力**：检索让它不瞎编、工具让它算得准、调度让它会分工，而且每一步都有评测和数字兜底。这正是 AI 应用/工程岗要的东西。」
